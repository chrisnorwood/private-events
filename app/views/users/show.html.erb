<h3><%= @user.name %>'s Hosting:</h3>
<table>
  <tr>
    <th>Title</th>
    <th>Time</th>
  </tr>
  <% if @user.created_events.any?
      @user.created_events.each do |event| %>
        <tr>
          <td><%= link_to event.title, event_url(event)%></td>
          <td><%= event.time %></td>
        </tr>
  <%  end
    else %>
    <tr>
      <td>No hosted events found!</td>
    </tr>
  <% end %>
</table>

<h3><%= @user.name %>'s Attending:</h3>
<table>
  <tr>
    <th>Title</th>
    <th>Time</th>
  </tr>
  <% if @user.events.any?
      @user.events.each do |event| %>
        <tr>
          <td><%= link_to event.title, event_url(event)%></td>
          <td><%= event.time %></td>
        </tr>
  <%  end
    else %>
    <tr>
      <td>No attending events found!</td>
    </tr>
  <% end %>
</table>